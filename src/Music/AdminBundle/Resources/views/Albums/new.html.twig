{% extends 'MusicAdminBundle::layout.html.twig' %}

{% block body -%}
    <h1>Albums creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_albums') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
